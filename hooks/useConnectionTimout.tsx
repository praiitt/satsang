// Deprecated timeout logic compatible with older SDKs
// Simplified or disabled for now to ensure stability
export function useConnectionTimeout(timout = 20_000) {
  // connection timeout logic temporarily disabled for compatibility with stable SDK
  // TODO: Re-implement using basic participant events if needed
}
